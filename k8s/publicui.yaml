apiVersion: apps/v1
kind: Deployment
metadata:
  name: publicui
  namespace: multiplai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: publicui
  template:
    metadata:
      labels:
        app: publicui
    spec:
      imagePullSecrets:
        - name: ocir-pull-secret
      containers:
        - name: publicui
          image: gru.ocir.io/grvzruvkeyil/multiplai/publicui:latest
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Production
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: publicui
  namespace: multiplai
spec:
  selector:
    app: publicui
  ports:
    - port: 8080
      targetPort: 8080
